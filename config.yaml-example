email:
    host: smtp.host.com
    port: 465
    user: user@host.com
    password: your_password

server:
    port: 8080

tls:
    enable: true
    key: xx.key
    cert: xx.pem

auth:
    key: secretKeyxxx